<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    {{! <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script> }}
    <link href="/css/style.css" rel="stylesheet" />
    {{! boxicons }}
    <link
      href="https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css"
      rel="stylesheet"
    />
    <title>The Tech Blog</title>
  </head>
  <body>

    <header>
      <style>
        nav { position: relative; margin: 30px auto 0; width: 335px; height:
        55px; background: #34495e; border-radius: 8px; font-size: 0; box-shadow:
        0 2px 3px 0 rgba(0,0,0,0.1); } nav a { font-size: 15px; text-transform:
        uppercase; color: white; text-decoration: none; line-height: 50px;
        position: relative; z-index: 1; display: inline-block; text-align:
        center; } a:nth-child(1) { width: 100px; } nav .start-home,
        a:nth-child(1):hover~.animation{ width: 100; left: 0; } a:nth-child(2) {
        width: 125px; } nav .start-dashboard, a:nth-child(2):hover~.animation{
        width: 125px; left: 100px; } a:nth-child(3) { width: 110px; } nav
        .start-log, a:nth-child(3):hover~.animation{ width: 110px; left: 225px;
        } nav .animation{ position: absolute; height: 100%; top: 0; z-index: 0;
        background-color: #1abc9c; border-radius: 8px; transition: all 0.5s ease
        0s; }

      </style>
      <nav>
        <a href="/">Home</a>
        <a href="/api/dashboard">Dashboard</a>
        {{#if logged_in}}
          <a href="#" id="logout">Logout</a>
        {{else}}
          <a href="/api/users">LogIn</a>
        {{/if}}
        <div class="animation start-home"></div>
      </nav>
      <!--dynamically generating a page title-->
      <div class="title">
        {{pageTitle}}
      </div>
    </header>
    <main>
      <!-- render the sub layout-->
      {{{body}}}
    </main>
    <!-- Render script for logged in users only -->
    {{#if logged_in}}
      <script src="/js/logout.js"></script>
    {{/if}}
  </body>

</html>